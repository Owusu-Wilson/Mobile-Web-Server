import { StyleSheet, Text, View } from "react-native";
import React from "react";

const InternetServer = () => {
  return (
    <View>
      <Text>InternetServer</Text>
    </View>
  );
};

export default InternetServer;

const styles = StyleSheet.create({});
